{"id": "T01-l1-memcasecmp", "fid": "src/nxt_string.c::nxt_memcasecmp", "tier": "L1", "template": "T-bounds", "trap": false, "signature": "nxt_int_t nxt_memcasecmp(const void *p1, const void *p2, size_t length)", "base_body_sha": "3a02a1a8dce866d5ef5554e7dc46b948a864aecb33eb5a0478aba5d87c552c68", "ccn": 3, "nloc": 19, "substitutes_for": "nxt_unit_memcasecmp", "substitution_reason": "nxt_unit_memcasecmp is defined in src/nxt_unit.c, which the CODE stream is editing today (nxt_unit_mmap_read lives in the same file); nxt_memcasecmp in src/nxt_string.c is the same case-insensitive-compare shape, in a file nobody else is touching today.", "known_issue": "No unit test exists for this helper anywhere in the tree (src/test/nxt_string_test.c covers other nxt_string.c helpers but not this one). Add a standalone test covering: identical strings, a case-only difference in the very last byte (folded equal), a difference in the first byte, and length == 0 (must return 0 without dereferencing p1 or p2). No behavior change is expected if all four cases already pass -- this is a coverage-only task unless your test finds otherwise.", "instruction": "Function: nxt_memcasecmp(const void *p1, const void *p2, size_t length) in src/nxt_string.c. It case-folds and compares `length` bytes from p1 and p2, byte by byte, and returns the signed difference of the first differing folded pair, or 0. Read the known_issue above. Add the missing test coverage it describes. If, and only if, one of those cases actually fails against the current body, make the minimal fix to pass it -- otherwise return the function byte-identical to how you found it.", "acceptance": {"gates": ["G1", "G5"], "tests_required": true, "test_kind": "standalone C (gcc, no test harness wiring)", "note": "G1 on gcc is enough for this tier; G5 (ast-grep) catches any accidental raw-copy-loop / unchecked-length pattern this task should not introduce."}, "expected_outcome_notes": "Not a trap, but low-drama: the four listed cases already pass against the unmodified function. The correct edit is 'add the test, no functional change'. A reviewer should reject any version that also rewrites the loop 'for clarity' or changes the return value's exact magnitude (only its sign is part of the contract, but changing the magnitude anyway is an unrequested behavior change).", "context_chars_est": 6810, "context_tokens_est": 1702, "test_skeleton": "/*\n * Minimal standalone C test skeleton for src/nxt_string.c::nxt_memcasecmp.\n *\n * nxt_memcasecmp has external linkage (it is not `static`), so this\n * skeleton includes nxt_main.h and links against build/lib/libnxt.a like\n * any other standalone C test in this harness -- see tools/factory/\n * README.md's \"harness v2\" section for the sanctioned method and why the\n * TU-include pattern below is NOT needed for this particular function.\n */\n\n#include <nxt_main.h>\n#include <stdio.h>\n\nint\nmain(void)\n{\n    /* placeholder check -- replace with the real assertions from the\n     * card's known_issue before submitting */\n    if (nxt_memcasecmp(\"a\", \"a\", 1) != 0) {\n        return 1;\n    }\n\n    printf(\"ok\\n\");\n    return 0;\n}\n"}
{"id": "T02-l1-fields-hash", "fid": "src/nxt_http_parse.c::nxt_http_fields_hash", "tier": "L1", "template": "T-scope", "trap": false, "signature": "nxt_int_t nxt_http_fields_hash(nxt_lvlhsh_t *hash, nxt_http_field_proc_t items[], nxt_uint_t count)", "base_body_sha": "cb9868c8a24e84a082f6e386b7be9f4b3ccc4bfc1381fd50efd6e9578aee27c9", "ccn": 4, "nloc": 29, "substitutes_for": "nxt_unit_field_hash", "substitution_reason": "nxt_unit_field_hash is defined in src/nxt_unit.c (same collision as above). nxt_http_fields_hash in src/nxt_http_parse.c is the same shape -- fold-and-hash each name in an array, insert into a level hash table -- in a stable file.", "known_issue": "Builds a level hash table from items[], lower-casing each name byte and folding it into a hash before nxt_lvlhsh_insert(). lhq.replace is 0, so a duplicate name (including a case-only duplicate) makes that insert fail and the whole function return NXT_ERROR -- by design, since a protocol module's own items[] should never repeat a field name, but nothing currently tests that this actually happens rather than silently overwriting the first entry. Add a test for: (a) a normal items[] populates the table so every name can be looked up afterward, and (b) two case-variant duplicate names (e.g. \"Host\" and \"host\") in items[] make the call return NXT_ERROR.", "instruction": "Function: nxt_http_fields_hash(nxt_lvlhsh_t *hash, nxt_http_field_proc_t items[], nxt_uint_t count) in src/nxt_http_parse.c. Read the known_issue above and add the described test coverage. Fix the function only if one of those two cases does not already behave as described; otherwise return it unchanged.", "acceptance": {"gates": ["G1", "G5"], "tests_required": true, "test_kind": "standalone C (gcc, no test harness wiring)", "note": "Same rationale as T01: cheap, no ASan needed for this tier."}, "expected_outcome_notes": "The duplicate-rejection behavior is already correct (it falls out of lhq.replace = 0); the task is coverage, not a fix. A reviewer should check the test actually asserts NXT_ERROR for case (b), not just that the call 'does something'.", "context_chars_est": 7591, "context_tokens_est": 1897, "test_skeleton": "/*\n * Minimal standalone C test skeleton for\n * src/nxt_http_parse.c::nxt_http_fields_hash.\n *\n * nxt_http_fields_hash has external linkage, so this skeleton includes\n * nxt_main.h plus the header that declares it (nxt_http_parse.h) and\n * links against build/lib/libnxt.a -- see tools/factory/README.md's\n * \"harness v2\" section.\n */\n\n#include <nxt_main.h>\n#include <nxt_http_parse.h>\n#include <string.h>\n#include <stdio.h>\n\nint\nmain(void)\n{\n    nxt_lvlhsh_t  hash;\n\n    memset(&hash, 0, sizeof(hash));\n\n    /* placeholder check -- an empty items[]/count == 0 call must\n     * succeed trivially; replace with the real assertions from the\n     * card's known_issue */\n    if (nxt_http_fields_hash(&hash, NULL, 0) != NXT_OK) {\n        return 1;\n    }\n\n    printf(\"ok\\n\");\n    return 0;\n}\n"}
{"id": "T03-l1-json-position-TRAP", "fid": "src/nxt_conf.c::nxt_conf_json_position", "tier": "L1", "template": "T-bounds", "trap": true, "signature": "void nxt_conf_json_position(u_char *start, const u_char *pos, nxt_uint_t *line, nxt_uint_t *column)", "base_body_sha": "947cf656e2dba7c509ab58966a5c9f57d75edab5eca75aa81856b446b9db6893", "ccn": 4, "nloc": 20, "substitutes_for": "nxt_unit_snprint_prefix", "substitution_reason": "nxt_unit_snprint_prefix is the plan's designated L1 trap, but it is defined in src/nxt_unit.c, the same file as the actively edited nxt_unit_mmap_read. nxt_conf_json_position in src/nxt_conf.c -- a small, already-correct position-computing helper with the same 'looks fixable, isn't' shape -- takes over as the L1 trap.", "known_issue": null, "instruction": "Function: nxt_conf_json_position(u_char *start, const u_char *pos, nxt_uint_t *line, nxt_uint_t *column) in src/nxt_conf.c. It walks from `start` to `pos` counting newlines to find a 1-based line number, then calls nxt_utf8_length() on the bytes since the last newline to get a 1-based column. It only writes *line/*column when nxt_utf8_length() did not return -1 (invalid UTF-8). Decide whether this function has a real defect worth fixing. If you find one, fix it with the minimal change and add a test that fails on the old body. If you do not, say so and add a test that pins the current (correct) behavior instead of changing anything.", "acceptance": {"gates": ["G1", "G5"], "tests_required": true, "test_kind": "standalone C (gcc, no test harness wiring)", "note": "Trap card: G1+G5 only need to prove the no-op compiles and lints clean."}, "expected_outcome_notes": "TRAP: correct answer is no semantic change. *line/*column are left untouched (whatever the caller initialized them to) only on invalid UTF-8, which is the intended fail-safe -- a caller that seeds them with a sane default before calling this is exactly why that branch does not also write a sentinel. An over-eager executor may 'fix' the seemingly-uninitialized-looking output on the invalid-UTF-8 path, or rewrite the newline-count loop to use nxt_memchr, changing nothing observable but failing the no-op check. Reviewer should reject any behavioral or even structural rewrite here.", "context_chars_est": 6845, "context_tokens_est": 1711, "test_skeleton": "/*\n * Minimal standalone C test skeleton for\n * src/nxt_conf.c::nxt_conf_json_position.\n *\n * nxt_conf_json_position has external linkage, so this skeleton includes\n * nxt_main.h plus nxt_conf.h and links against build/lib/libnxt.a -- see\n * tools/factory/README.md's \"harness v2\" section.\n */\n\n#include <nxt_main.h>\n#include <nxt_conf.h>\n#include <stdio.h>\n\nint\nmain(void)\n{\n    u_char      buf[1];\n    nxt_uint_t  line, column;\n\n    buf[0] = 'x';\n    line = 0;\n    column = 0;\n\n    /* placeholder check -- pos == start is always line 1, column 1;\n     * replace with the real assertions from the card's known_issue */\n    nxt_conf_json_position(buf, buf, &line, &column);\n\n    if (line != 1 || column != 1) {\n        return 1;\n    }\n\n    printf(\"ok\\n\");\n    return 0;\n}\n"}
{"id": "T04-l1-mmap-get-method", "fid": "src/nxt_port_memory.c::nxt_port_mmap_get_method", "tier": "L1", "template": "T-scope", "trap": false, "signature": "nxt_port_method_t nxt_port_mmap_get_method(nxt_task_t *task, nxt_port_t *port, nxt_buf_t *b)", "base_body_sha": "a1d07443776ed05f0bb7464d987f0aca6dcb178d4ce7763250bf2dcc698da0b9", "ccn": null, "nloc": null, "substitutes_for": null, "substitution_reason": null, "known_issue": "Scans a nxt_buf_t chain deciding NXT_PORT_METHOD_ANY -> ..._MMAP/..._PLAIN, falling back to plain and breaking out of the loop as soon as both kinds are seen, in either order (mmap-then-plain and plain-then-mmap both break to plain correctly by inspection). Buffers with nxt_buf_used_size(b) == 0 are skipped for method selection via `continue`. None of this is under test: add coverage proving both mixed-order chains converge on NXT_PORT_METHOD_PLAIN, that an all-empty chain returns NXT_PORT_METHOD_ANY, and that an empty buffer in the middle, first, or last position of an otherwise-uniform chain does not change the result.", "instruction": "Function: nxt_port_mmap_get_method(nxt_task_t *task, nxt_port_t *port, nxt_buf_t *b) in src/nxt_port_memory.c. This is IPC-code-adjacent (mmap transfer method selection) but is a distinct function from nxt_port_mmap_read, which the CODE stream is editing elsewhere in this same file today -- do not assume anything about that function's current state, and do not touch anything outside this function's own body. Add the test coverage the known_issue describes. Fix only if your tests find an actual mismatch against the description above.", "acceptance": {"gates": ["G1", "G5"], "tests_required": true, "test_kind": "standalone C (gcc, no test harness wiring)", "note": "Kept to G1+G5 deliberately -- this file has a live edit elsewhere today, so this card's gate run should not attempt a full G3 ASan build that would also compile nxt_port_mmap_read's in-flux body."}, "expected_outcome_notes": "All three properties in known_issue already hold by inspection of the loop; this is a coverage task. A reviewer should confirm the test actually constructs a real nxt_buf_t chain (not a mocked call) since nxt_buf_used_size()/nxt_buf_is_port_mmap() read real buffer fields.", "context_chars_est": 12959, "context_tokens_est": 3239, "test_skeleton": "/*\n * Minimal standalone C test skeleton for\n * src/nxt_port_memory.c::nxt_port_mmap_get_method.\n *\n * nxt_port_mmap_get_method has external linkage, so this skeleton\n * includes nxt_main.h plus nxt_port_memory.h and links against\n * build/lib/libnxt.a -- see tools/factory/README.md's \"harness v2\"\n * section.\n */\n\n#include <nxt_main.h>\n#include <nxt_port_memory.h>\n#include <stdio.h>\n\nint\nmain(void)\n{\n    nxt_port_method_t  m;\n\n    /* placeholder check -- an empty (NULL) buffer chain must return\n     * NXT_PORT_METHOD_ANY without dereferencing task/port; replace with\n     * the real assertions from the card's known_issue */\n    m = nxt_port_mmap_get_method(NULL, NULL, NULL);\n    if (m != NXT_PORT_METHOD_ANY) {\n        return 1;\n    }\n\n    printf(\"ok\\n\");\n    return 0;\n}\n"}
{"id": "T05-l2-chunk-range-valid-TRAP", "fid": "src/nxt_port_memory_int.h::nxt_port_mmap_chunk_range_valid", "tier": "L2", "template": "T-bounds", "trap": true, "signature": "nxt_bool_t nxt_port_mmap_chunk_range_valid(nxt_chunk_id_t chunk_id, uint32_t size, size_t *nchunks)", "base_body_sha": "4e90547091b7bb2b477b31c5f5ac36a76299ac2f4cacf14fdc86ad7e92d81d18", "ccn": 4, "nloc": 17, "substitutes_for": null, "substitution_reason": "Kept as the plan's designated L2 trap: it is defined in src/nxt_port_memory_int.h, not in src/nxt_port_memory.c (where the actively-edited nxt_port_mmap_read lives), so it is stable today despite the IPC-sounding name.", "known_issue": null, "instruction": "Function: nxt_port_mmap_chunk_range_valid(nxt_chunk_id_t chunk_id, uint32_t size, size_t *nchunks) in src/nxt_port_memory_int.h (an nxt_inline helper -- match the 'signature' field, which omits the nxt_inline qualifier, per apply_edit.py's docstring). It computes nchunks = ceil(size / PORT_MMAP_CHUNK_SIZE), always writes *nchunks, then rejects chunk_id >= PORT_MMAP_CHUNK_COUNT and rejects nchunks > PORT_MMAP_CHUNK_COUNT - chunk_id (computed as a size_t subtraction after chunk_id has already been range-checked against PORT_MMAP_CHUNK_COUNT above it, so it cannot underflow). Decide whether this bounds check is actually sound for every chunk_id/size pair a peer could send, including size == 0 and size at the exact PORT_MMAP_CHUNK_SIZE boundary. Fix it only if you find a real gap; otherwise add a test pinning the boundary cases and return the function unchanged.", "acceptance": {"gates": ["G1", "G5"], "tests_required": true, "test_kind": "standalone C (gcc, no test harness wiring)", "note": "Trap card, same shape as T03."}, "expected_outcome_notes": "TRAP: correct answer is no semantic change. size == 0 gives nchunks == 0, which always satisfies 'n > COUNT - chunk_id' being false for any valid chunk_id, so it is accepted -- correctly, since a zero-length chunk claims no chunk slots. The size_t subtraction is guarded by the chunk_id range check immediately above it, so it cannot underflow. An over-eager executor may add a redundant `size == 0` special case or an extra cast 'for safety'. Reviewer should reject any behavioral change.", "context_chars_est": 6673, "context_tokens_est": 1668, "test_skeleton": "/*\n * Minimal standalone C test skeleton for\n * src/nxt_port_memory_int.h::nxt_port_mmap_chunk_range_valid.\n *\n * This function is `nxt_inline` (static inline, defined in the header),\n * so plain inclusion of the header after nxt_main.h already gives this\n * TU a full definition to call -- no TU-include of a .c file is needed\n * or possible here (there is no .c file for it). See tools/factory/\n * README.md's \"harness v2\" section for when the TU-include pattern\n * below-style is required instead (a `static` function defined in a\n * .c file).\n */\n\n#include <nxt_main.h>\n#include <nxt_port_memory_int.h>\n#include <stdio.h>\n\nint\nmain(void)\n{\n    size_t      nchunks;\n    nxt_bool_t  ok;\n\n    /* placeholder check -- a zero-size chunk at chunk_id 0 is valid\n     * with nchunks == 0; replace with the real boundary assertions\n     * from the card's known_issue */\n    ok = nxt_port_mmap_chunk_range_valid(0, 0, &nchunks);\n    if (!ok || nchunks != 0) {\n        return 1;\n    }\n\n    printf(\"ok\\n\");\n    return 0;\n}\n"}
{"id": "T06-l2-parse-field-name", "fid": "src/nxt_http_parse.c::nxt_http_parse_field_name", "tier": "L2", "template": "T-bounds", "trap": false, "signature": "nxt_int_t nxt_http_parse_field_name(nxt_http_request_parse_t *rp, u_char **pos, const u_char *end)", "base_body_sha": "09f59397baba59147cb0a713d923ee2451a0f2edd03eed1e3b7d134f4d67b077", "ccn": 8, "nloc": 59, "substitutes_for": "nxt_unit_buf_read", "substitution_reason": "nxt_unit_buf_read is defined in src/nxt_unit.c (collision, see T01). nxt_http_parse_field_name in src/nxt_http_parse.c is a peer-controlled-bytes buffer scan of matching L2 complexity (ccn 8), in a file the CODE stream is not touching today.", "known_issue": "Scans a HTTP field name with an 8-byte-unrolled fast loop and a 1-byte tail loop, using a 256-entry classification table; a disallowed byte (table value '\\0') jumps to name_end via `p = &(ch)` from inside the unrolled macro. There is no test pinning this at the exact points where behavior could differ between the two loops: a name length of exactly 0, 7, 8, or 9 bytes (the 8-wide loop's boundary), and a disallowed byte (e.g. NUL or a raw CR) landing on the last iteration of an 8-wide block vs. the first iteration of the next one. Add tests for these; fix only if one exposes a real mismatch against the field name the caller should see.", "instruction": "Function: nxt_http_parse_field_name(nxt_http_request_parse_t *rp, u_char **pos, const u_char *end) in src/nxt_http_parse.c. You will need a minimal nxt_http_request_parse_t set up correctly enough to drive this function directly (its field_name/field_hash/discard_unsafe_fields/skip_field members -- see src/nxt_http_parse.h for the struct, or slice.py's 'types referenced' section). Read the known_issue above and add the described boundary tests. Fix the function only if a test fails against its current body.", "acceptance": {"gates": ["G1", "G2", "G5"], "tests_required": true, "test_kind": "standalone C (gcc, no test harness wiring) or a pytest under test/ if the case is easier to drive at the request layer", "note": "G2 (build + pytest slice) is added at this tier since a field-name parsing test may be easier to write as a small HTTP request through the real server than as a hand-built nxt_http_request_parse_t."}, "expected_outcome_notes": "By inspection the fast and tail loops agree (they share the same test macro), so this is most likely a coverage-only task. A reviewer should check the boundary-length tests actually hit both loops (i.e. include at least one length >= 8 and one < 8), not just repeat the same short case.", "context_chars_est": 12310, "context_tokens_est": 3077, "test_skeleton": "/*\n * Minimal standalone C test skeleton for\n * src/nxt_http_parse.c::nxt_http_parse_field_name.\n *\n * nxt_http_parse_field_name is `static`, so it cannot be linked from a\n * separate translation unit. The sanctioned method (see tools/factory/\n * README.md's \"harness v2\" section) is to #include the whole .c file\n * directly, compiled with -I src and linked against build/lib/libnxt.a\n * as usual: the archive will not pull in the duplicate nxt_http_parse.o\n * member since this test's own object file already defines every\n * symbol nxt_http_parse.c provides.\n *\n * Do NOT remove `static`, change nxt_http_parse_field_name's signature,\n * or copy its body into this file instead of including the TU -- all\n * three are out of scope for this card and will be rejected. Do not\n * re-implement the function's parsing logic here either; drive the\n * real static function through a properly initialized\n * nxt_http_request_parse_t (see src/nxt_http_parse.h).\n */\n\n#include \"nxt_http_parse.c\"\n#include <stdio.h>\n\nint\nmain(void)\n{\n    /* placeholder check -- replace with real boundary-length assertions\n     * (0/7/8/9-byte field names) against nxt_http_parse_field_name(),\n     * driven through a real nxt_http_request_parse_t, per the card's\n     * known_issue */\n    if (1 != 1) {\n        return 1;\n    }\n\n    printf(\"ok\\n\");\n    return 0;\n}\n"}
{"id": "T07-l2-parse-field-end", "fid": "src/nxt_http_parse.c::nxt_http_parse_field_end", "tier": "L2", "template": "T-scope", "trap": false, "signature": "nxt_int_t nxt_http_parse_field_end(nxt_http_request_parse_t *rp, u_char **pos, const u_char *end)", "base_body_sha": "f776e7d347732d361ddee1e5f44aa71a41bb5709c097d09ea734e81a45078b24", "ccn": 10, "nloc": 52, "substitutes_for": "nxt_unit_request_read", "substitution_reason": "nxt_unit_request_read is defined in both src/nxt_php_sapi.c and src/nxt_unit.c; the nxt_unit.c copy collides with today's nxt_unit_mmap_read edits. nxt_http_parse_field_end in src/nxt_http_parse.c reads/dispatches a parsed field into request state with matching L2 complexity (ccn 10), in a stable file.", "known_issue": "On seeing the CRLF that ends one header field, this function either stores it into rp->inline_fields[] (while num_inline_fields < 16) or, once that fills up, lazily creates rp->fields (an nxt_list_t) and appends there instead; a field with rp->skip_field set is dropped without occupying either slot. There is no test pinning the num_inline_fields == 15/16/17 transition (last inline slot, first heap-list slot, second heap-list slot), nor a skipped field interleaved with kept ones (the skip must not consume an inline slot or shift later fields' positions). Add these tests; fix only if one exposes a real mismatch.", "instruction": "Function: nxt_http_parse_field_end(nxt_http_request_parse_t *rp, u_char **pos, const u_char *end) in src/nxt_http_parse.c. Drive it directly with a hand-built nxt_http_request_parse_t (a real nxt_mp_t pool for rp->mem_pool, since the 17th field allocates an nxt_list_t from it), or write the case as a pytest sending a request with >16 headers, one of which the server is known to skip. Read the known_issue above and add the described tests; fix the function only if a test fails against its current body.", "acceptance": {"gates": ["G1", "G2", "G5"], "tests_required": true, "test_kind": "standalone C (gcc, no test harness wiring) or a pytest under test/", "note": "Same G2 rationale as T06."}, "expected_outcome_notes": "By inspection the 16/17-field transition and the skip-field path are both handled correctly; this is most likely coverage-only. A reviewer should check the test actually asserts on *count* and *identity* of stored fields (not just 'no crash') across the 15/16/17 boundary.", "context_chars_est": 10211, "context_tokens_est": 2552, "test_skeleton": "/*\n * Minimal standalone C test skeleton for\n * src/nxt_http_parse.c::nxt_http_parse_field_end.\n *\n * nxt_http_parse_field_end is `static`; this skeleton uses the same\n * sanctioned TU-include method as T06/T10/T12 (see tools/factory/\n * README.md's \"harness v2\" section): #include the whole .c file,\n * compile with -I src, link against build/lib/libnxt.a. The archive\n * will not pull in the duplicate nxt_http_parse.o member.\n *\n * Do NOT remove `static`, change the function's signature, or copy its\n * body into this file -- all three are out of scope and are rejected.\n * Do not re-implement its inline-fields/heap-list bookkeeping here\n * either; drive the real static function through a properly\n * initialized nxt_http_request_parse_t and a real nxt_mp_t pool for\n * rp->mem_pool, per the card's known_issue.\n */\n\n#include \"nxt_http_parse.c\"\n#include <stdio.h>\n\nint\nmain(void)\n{\n    /* placeholder check -- replace with real assertions covering the\n     * num_inline_fields == 15/16/17 transition and a skipped field, per\n     * the card's known_issue */\n    if (1 != 1) {\n        return 1;\n    }\n\n    printf(\"ok\\n\");\n    return 0;\n}\n"}
{"id": "T08-l2-json-escape-pair", "fid": "src/nxt_conf.c::nxt_conf_json_escape_length", "tier": "L2", "template": "T-ownership", "trap": false, "signature": "size_t nxt_conf_json_escape_length(u_char *p, size_t size)", "base_body_sha": "e199dba14112dc0534a8d680c0b01a1c656e3ff07eab91123edbdbb268b9812e", "ccn": 10, "nloc": 26, "companion_fid": "src/nxt_conf.c::nxt_conf_json_escape", "companion_signature": "u_char * nxt_conf_json_escape(u_char *dst, u_char *src, size_t size)", "companion_body_sha": "4bb10575d3e3352067e04fbd28004891a0ec77b908c96af58321744a4e615cab", "companion_note": "nxt_conf_json_escape() is the sizing counterpart's write side: the caller calls nxt_conf_json_escape_length() first to size its destination buffer, then nxt_conf_json_escape() writes into exactly that many bytes. Per the T-ownership template, treat nxt_conf_json_escape() as a fixed, correct read-only reference -- you may only edit nxt_conf_json_escape_length() in this card.", "substitutes_for": null, "substitution_reason": null, "known_issue": "nxt_conf_json_escape_length(p, size) and nxt_conf_json_escape() must agree, byte for byte, on how many output bytes each input byte produces -- the caller allocates exactly what escape_length() returns and then escape() writes into it with no further bounds check. They currently agree (both special-case '\\\\'/'\"' as 1 extra byte, the five named control chars \\n\\r\\t\\b\\f as 1 extra byte, and the remaining C0 control bytes 0x00-0x1F as a 6-byte \\u00XX escape, i.e. 5 extra), but nothing tests that agreement -- only that either function individually 'looks right'. Add a test that, for a range of inputs including all 32 C0 control bytes, backslash, quote, and ordinary printable bytes, computes nxt_conf_json_escape_length() then calls nxt_conf_json_escape() into a buffer allocated to exactly that size, and asserts the write never disagrees with the size (use a canary byte pattern or an exact-size buffer plus an ASan-clean run to catch an overrun).", "instruction": "Function: nxt_conf_json_escape_length(u_char *p, size_t size) in src/nxt_conf.c. Its sibling nxt_conf_json_escape() is described above and must not be touched by this card. Read the known_issue above and add the cross-consistency test it describes. Fix nxt_conf_json_escape_length() only if the test finds it disagreeing with nxt_conf_json_escape() for some byte value; if you find a real disagreement, the fix must be to correct escape_length()'s count to match escape()'s actual output, not the other way around, since escape() is fixed for this card.", "acceptance": {"gates": ["G1", "G3", "G5"], "tests_required": true, "test_kind": "standalone C (gcc, ideally also run once under ASan/UBSan per G3)", "note": "G3 (ASan/UBSan) is included here specifically because a sizing/writing mismatch is exactly a heap-buffer-overflow, which a plain assertion-based test might miss if the overrun lands in unused heap slack; ASan will not."}, "expected_outcome_notes": "By hand-checking every branch, the two functions already agree for all 256 byte values (5 extra chars per C0-minus-the-five-named path is exactly sizeof(\"\\\\u001F\") - 2 = 5). This is a coverage-only task. A reviewer should confirm the test really allocates the destination at exactly escape_length()'s answer (not size+slack) so an ASan run would actually catch a regression.", "context_chars_est": 9106, "context_tokens_est": 2276, "test_skeleton": "/*\n * Minimal standalone C test skeleton for\n * src/nxt_conf.c::nxt_conf_json_escape_length.\n *\n * nxt_conf_json_escape_length is `static`; this skeleton uses the\n * sanctioned TU-include method (see tools/factory/README.md's\n * \"harness v2\" section): #include the whole .c file, compile with\n * -I src, link against build/lib/libnxt.a. This also brings in the\n * read-only companion nxt_conf_json_escape() from the same TU, which\n * this card's real test must call for its cross-consistency check.\n *\n * Do NOT remove `static` from either function, change either\n * signature, or copy either body into this file -- all are out of\n * scope. Do not re-implement the escaping table here; call both real\n * functions and compare their answers, per the card's known_issue.\n */\n\n#include \"nxt_conf.c\"\n#include <stdio.h>\n\nint\nmain(void)\n{\n    u_char  buf[3];\n    size_t  n;\n\n    buf[0] = 'a'; buf[1] = 'b'; buf[2] = 'c';\n\n    /* placeholder check -- replace with the real cross-consistency test\n     * against nxt_conf_json_escape() over all 256 byte values, per the\n     * card's known_issue */\n    n = nxt_conf_json_escape_length(buf, 3);\n    if (n != 3) {\n        return 1;\n    }\n\n    printf(\"ok\\n\");\n    return 0;\n}\n"}
{"id": "T09-l2-json-skip-space", "fid": "src/nxt_conf.c::nxt_conf_json_skip_space", "tier": "L2", "template": "T-scope", "trap": false, "signature": "u_char * nxt_conf_json_skip_space(u_char *start, const u_char *end)", "base_body_sha": "8c70674d0bab752226d5fe2e0026476b21429a594147566ddf6b7c9256a952d8", "ccn": 19, "nloc": 65, "substitutes_for": null, "substitution_reason": "Not a plan sample originally, but explicitly called out in the task brief: moved into the L2 sample at ccn 19, the highest ccn in this batch outside L3, and it lives in the stable src/nxt_conf.c.", "known_issue": "A 5-state machine (sw_normal / sw_after_slash / sw_single_comment / sw_multi_comment / sw_after_asterisk) skips whitespace and //-line and /*-block comments in the extended JSON config format. On reaching `end` mid-comment (state != sw_normal), it returns `start` -- rewound to wherever the unterminated comment began -- rather than `p` (== end), so the caller's parser re-reports the error at the comment's start, not at end-of-buffer. Nothing tests this rewind, nor the `sw_after_asterisk` self-loop on a run of `*` characters before the closing `/`, nor a bare trailing '/' with nothing after it (state stays sw_after_slash, same rewind-to-start behavior applies). Add tests for: a `//` comment unterminated at EOF, a `/* ... ` block unterminated at EOF, a run of `**/` closing a block correctly, and a lone trailing '/'. Fix only if one shows the rewind is wrong.", "instruction": "Function: nxt_conf_json_skip_space(u_char *start, const u_char *end) in src/nxt_conf.c. Read the known_issue above and add the described tests, calling the function directly with small in-memory buffers. Fix only if a test exposes a real defect; keep the state machine's states and transitions otherwise exactly as they are.", "acceptance": {"gates": ["G1", "G2", "G5"], "tests_required": true, "test_kind": "standalone C (gcc, no test harness wiring)", "note": "G2 kept in case a test is easier expressed via test_config_validation_errors.py-style config parsing."}, "expected_outcome_notes": "The rewind-to-`start` behavior on an unterminated comment is intentional and correct (it lets the caller's error message point at the comment, not at EOF). This is a coverage-only task; a reviewer should reject a change that makes the function return `p` (== end) instead on this path, even though that looks more 'consistent' at a glance.", "context_chars_est": 7817, "context_tokens_est": 1954, "test_skeleton": "/*\n * Minimal standalone C test skeleton for\n * src/nxt_conf.c::nxt_conf_json_skip_space.\n *\n * nxt_conf_json_skip_space is `static`; this skeleton uses the\n * sanctioned TU-include method (see tools/factory/README.md's\n * \"harness v2\" section): #include the whole .c file, compile with\n * -I src, link against build/lib/libnxt.a.\n *\n * Do NOT remove `static`, change the function's signature, or copy its\n * body/state machine into this file (as a \"mirror\" or otherwise) -- all\n * three are out of scope and are rejected; call the real function\n * directly, as below.\n */\n\n#include \"nxt_conf.c\"\n#include <stdio.h>\n\nint\nmain(void)\n{\n    u_char  buf[3];\n    u_char  *p;\n\n    buf[0] = ' '; buf[1] = ' '; buf[2] = ' ';\n\n    /* placeholder check -- replace with the real unterminated-comment\n     * and rewind-to-start assertions from the card's known_issue */\n    p = nxt_conf_json_skip_space(buf, buf + 3);\n    if (p != buf + 3) {\n        return 1;\n    }\n\n    printf(\"ok\\n\");\n    return 0;\n}\n"}
{"id": "T10-l2-lookup-field-end", "fid": "src/nxt_http_parse.c::nxt_http_lookup_field_end", "tier": "L2", "template": "T-bounds", "trap": false, "signature": "u_char * nxt_http_lookup_field_end(u_char *p, const u_char *end)", "base_body_sha": "9d70c0cb6e8a8111b3a5753ae8e5cad8e9719c5d33ef1d838d1ce43615e75a8d", "ccn": 7, "nloc": 42, "substitutes_for": null, "substitution_reason": null, "known_issue": "Scans forward for the first byte < 0x20 using 16-byte, then 4-byte, then a switch-based 3/2/1-byte tail, all via the same nxt_field_end_test_char macro. DEL (0x7F) is NOT < 0x20, so it is treated as an ordinary field-value byte and never triggers a stop here -- that is intentional (DEL rejection, if any, happens in the caller, nxt_http_parse_field_value). Nothing tests the 16/4/1-byte path split at its exact boundaries (buffer lengths 0, 1, 3, 4, 15, 16, 17 relative to `end`), nor that a control byte landing on the very last byte of a 16-wide or 4-wide block is still caught (not skipped past by the block advancing `p` by 16/4). Add tests for these lengths and boundary placements; fix only if one exposes a mismatch.", "instruction": "Function: nxt_http_lookup_field_end(u_char *p, const u_char *end) in src/nxt_http_parse.c. Call it directly with small in-memory buffers of the lengths named in known_issue, placing a control byte (e.g. 0x01) at each boundary position in turn. Add the described tests; fix only if a test fails against the current body.", "acceptance": {"gates": ["G1", "G5"], "tests_required": true, "test_kind": "standalone C (gcc, no test harness wiring)", "note": null}, "expected_outcome_notes": "By inspection every unrolled block re-tests every byte it advances past (the macro is applied to p[0]..p[15] before `p += 16`), so a control byte at block-position 15 is caught before the advance, not skipped. Coverage-only task. Reviewer should check the test suite actually places a hit at the *last* byte of a 16-block and a 4-block, not only at the first.", "context_chars_est": 7950, "context_tokens_est": 1987, "test_skeleton": "/*\n * Minimal standalone C test skeleton for\n * src/nxt_http_parse.c::nxt_http_lookup_field_end.\n *\n * nxt_http_lookup_field_end is `static`; this skeleton uses the\n * sanctioned TU-include method (see tools/factory/README.md's\n * \"harness v2\" section): #include the whole .c file, compile with\n * -I src, link against build/lib/libnxt.a.\n *\n * Do NOT remove `static`, change the function's signature, or copy its\n * body into this file -- all three are out of scope and are rejected;\n * call the real function directly, as below.\n */\n\n#include \"nxt_http_parse.c\"\n#include <stdio.h>\n\nint\nmain(void)\n{\n    u_char  buf[4];\n    u_char  *p;\n\n    buf[0] = 'a'; buf[1] = 'b'; buf[2] = 'c'; buf[3] = 'd';\n\n    /* placeholder check -- replace with the real 16/4/1-byte boundary\n     * assertions from the card's known_issue */\n    p = nxt_http_lookup_field_end(buf, buf + 4);\n    if (p != buf + 4) {\n        return 1;\n    }\n\n    printf(\"ok\\n\");\n    return 0;\n}\n"}
{"id": "T11-l2-json-print-object", "fid": "src/nxt_conf.c::nxt_conf_json_print_object", "tier": "L2", "template": "T-ownership", "trap": false, "signature": "u_char * nxt_conf_json_print_object(u_char *p, const nxt_conf_value_t *value, nxt_conf_json_pretty_t *pretty)", "base_body_sha": "3019d85ee8da87b6e0b4d81b2e12e5e9427736933677f7acbd8fefdfc0098ad4", "ccn": 10, "nloc": 48, "substitutes_for": "nxt_unit_response_add_field", "substitution_reason": "nxt_unit_response_add_field is defined in both src/nxt_php_sapi.c and src/nxt_unit.c; the nxt_unit.c copy collides with today's nxt_unit_mmap_read edits. nxt_conf_json_print_object in src/nxt_conf.c has the matching shape for T-ownership: it writes into a caller-owned buffer whose size was computed by a sibling length function (nxt_conf_json_object_length / nxt_conf_json_length), one member at a time, exactly like appending fields to a response.", "known_issue": "Writes a JSON object's '{', each 'name:value' member separated by ',', and the closing '}' into `p`, which the caller sized with nxt_conf_json_length() (which in turn calls nxt_conf_json_object_length() for an object value) -- this function never checks it is still inside that allocation. There is no test that an object with 0 members writes exactly '{}' (no comma, no pretty-printing newline attempt inside the empty `if (object->count != 0)` guard), nor that a 1-member object emits no trailing comma, matching object_length()'s count exactly for both cases. Add tests for 0-member and 1-member objects (and, if convenient, a 2-member one) that call nxt_conf_json_object_length()/nxt_conf_json_length() to size a buffer, then nxt_conf_json_print_object() into it, and assert the bytes actually written match the sized length exactly.", "instruction": "Function: nxt_conf_json_print_object(u_char *p, const nxt_conf_value_t *value, nxt_conf_json_pretty_t *pretty) in src/nxt_conf.c. Its sizing counterpart (nxt_conf_json_object_length/nxt_conf_json_length) is a fixed, correct read-only reference for this card -- do not edit it. Build small nxt_conf_value_t objects with nxt_conf_create_object / nxt_conf_set_member_string (see src/nxt_conf.h) to drive the 0/1/2-member cases from known_issue, with pretty == NULL. Fix nxt_conf_json_print_object() only if a test shows its output length disagrees with the sizing counterpart's answer.", "acceptance": {"gates": ["G1", "G3", "G5"], "tests_required": true, "test_kind": "standalone C (gcc, ideally also under ASan/UBSan per G3)", "note": "G3 for the same reason as T08: a sizing/writing mismatch here is a heap overrun, and this pairs a *_length() with a *_print() the same way."}, "expected_outcome_notes": "By inspection the empty-object and single-member cases already match their sizing counterpart exactly. Coverage-only task. Reviewer should check the test used `pretty == NULL` (matching nxt_conf_json_object_length()'s own unconditional counting) rather than accidentally exercising only the pretty-printing path, whose length accounting lives in a different function.", "context_chars_est": 9732, "context_tokens_est": 2433, "test_skeleton": "/*\n * Minimal standalone C test skeleton for\n * src/nxt_conf.c::nxt_conf_json_print_object.\n *\n * nxt_conf_json_print_object is `static`; this skeleton uses the\n * sanctioned TU-include method (see tools/factory/README.md's\n * \"harness v2\" section): #include the whole .c file, compile with\n * -I src, link against build/lib/libnxt.a. This also brings in the\n * read-only sizing counterparts nxt_conf_json_object_length() /\n * nxt_conf_json_length() from the same TU.\n *\n * Do NOT remove `static`, change the function's signature, or copy its\n * body into this file -- all three are out of scope. Do not\n * re-implement the sizing logic here either; build real\n * nxt_conf_value_t objects with nxt_conf_create_object() /\n * nxt_conf_set_member_string() (see src/nxt_conf.h) and compare the\n * sizing counterpart's answer against what this function actually\n * writes, per the card's known_issue.\n */\n\n#include \"nxt_conf.c\"\n#include <stdio.h>\n\nint\nmain(void)\n{\n    /* placeholder check -- replace with real 0/1/2-member\n     * length-vs-write assertions, per the card's known_issue */\n    if (1 != 1) {\n        return 1;\n    }\n\n    printf(\"ok\\n\");\n    return 0;\n}\n"}
{"id": "T12-l3-parse-field-value", "fid": "src/nxt_http_parse.c::nxt_http_parse_field_value", "tier": "L3", "template": "T-bounds", "trap": false, "signature": "nxt_int_t nxt_http_parse_field_value(nxt_http_request_parse_t *rp, u_char **pos, const u_char *end)", "base_body_sha": "62b7f35d404f18ca05294b4364a15791a393e1772002e18f8f565e5171a05353", "ccn": 15, "nloc": 55, "substitutes_for": null, "substitution_reason": null, "known_issue": "Parses a header field's value across possibly-multiple reads: skips leading space/tab, then repeatedly calls nxt_http_lookup_field_end() to find the next control byte, treating '\\r'/'\\n' as the end and a bare '\\t' as an embedded fold (kept, loop continues) and anything else < 0x20 as NXT_HTTP_PARSE_INVALID. On running out of buffer (nxt_slow_path(p == end)) mid-value it saves partial progress into rp->field_value.length/rp->handler and returns NXT_AGAIN, resuming via `p += rp->field_value.length` on the next call -- and only checks NXT_HTTP_MAX_FIELD_VALUE on that NXT_AGAIN path and again on the final completed-value path, meaning the check fires at least once per resumed read, not just once at the end. Trailing space/tab is trimmed after the value ends. There is no test that: (a) a value split across two reads right at the NXT_AGAIN boundary reassembles to the same field_value as one read would give, (b) a value that grows past NXT_HTTP_MAX_FIELD_VALUE is rejected on the very read that crosses the limit (not one read later), and (c) trailing whitespace is trimmed correctly when the whitespace itself straddles two reads. Add these three tests; fix only if one exposes a real mismatch.", "instruction": "Function: nxt_http_parse_field_value(nxt_http_request_parse_t *rp, u_char **pos, const u_char *end) in src/nxt_http_parse.c. This is the highest-tier card in this batch (L3): read the whole known_issue carefully, and slice.py this fid plus nxt_http_lookup_field_end and nxt_http_parse_field_end (its callee and its continuation) for the full picture before editing. Drive the split-read case by calling the function twice with the same rp and advancing `end` between calls (or, if simpler, write the case as a pytest that sends the request in two separate socket writes with a short delay). Add the three tests from known_issue; fix the function only if a test fails against its current body, and keep the fix scoped to this function only.", "acceptance": {"gates": ["G1", "G2", "G3", "G5"], "tests_required": true, "test_kind": "standalone C (gcc) for the reassembly/limit tests, or a pytest under test/ for the split-write case", "note": "Full gate set for L3: G3 (ASan/UBSan) because this function does pointer arithmetic across a resumable, attacker-controlled multi-read parse -- exactly the shape of past incidents in this codebase's git history (see the sprint plan's 'fragile files' table)."}, "expected_outcome_notes": "By careful reading, (a) and (c) already reassemble correctly (field_value.length carries the partial length across the NXT_AGAIN boundary and `p += rp->field_value.length` resumes from exactly there); (b) the length check on the NXT_AGAIN path already fires on the read that crosses the limit, not one read later, because it is evaluated against `p - start` at that same call. This is most likely a coverage-only task, but it is the one card in this batch a reviewer should read most carefully -- if any executor 'fixes' one of these paths, re-derive by hand whether the old or new behavior was actually correct before accepting or rejecting.", "context_chars_est": 11007, "context_tokens_est": 2751, "test_skeleton": "/*\n * Minimal standalone C test skeleton for\n * src/nxt_http_parse.c::nxt_http_parse_field_value.\n *\n * nxt_http_parse_field_value is `static`; this skeleton uses the\n * sanctioned TU-include method (see tools/factory/README.md's\n * \"harness v2\" section): #include the whole .c file, compile with\n * -I src, link against build/lib/libnxt.a. This also brings in its\n * callee nxt_http_lookup_field_end() and its continuation\n * nxt_http_parse_field_end() from the same TU.\n *\n * Do NOT remove `static`, change the function's signature, or copy its\n * body into this file -- all three are out of scope. Do not\n * re-implement the resumable multi-read parsing logic here either;\n * drive the real static function twice against the same rp, advancing\n * `end` between calls, to exercise the NXT_AGAIN split-read boundary\n * from the card's known_issue.\n */\n\n#include \"nxt_http_parse.c\"\n#include <stdio.h>\n\nint\nmain(void)\n{\n    /* placeholder check -- replace with the real split-read\n     * reassembly, over-limit, and split-whitespace assertions from\n     * the card's known_issue */\n    if (1 != 1) {\n        return 1;\n    }\n\n    printf(\"ok\\n\");\n    return 0;\n}\n"}
