id: sprintf-unbounded
language: c
severity: warning
message: >-
  sprintf() has no bound on the destination buffer. Use nxt_sprintf()/
  nxt_snprintf() (src/nxt_string.h), which take an explicit end pointer,
  instead.
rule:
  pattern: sprintf($DST, $$$)
